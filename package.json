{
  "name": "nyaa-editor",
  "version": "0.1.0",
  "private": true,
  "main": "src/desktop/main.js",
  "scripts": {
    "dev": "set NODE_ENV=development && electron .",
    "build": "npm run server:build && npm run web:build",
    "web:dev": "cd src/web && npm run dev",
    "web:build": "cd src/web && npm run build",
    "server:dev": "cd src/server && npm run dev",
    "server:build": "cd src/server && npm run build"
  },
  "dependencies": {
    "core-js": "^3.8.3",
    "dayjs": "^1.11.0"
  },
  "devDependencies": {
    "electron": "^18.0.4",
    "electron-devtools-installer": "^3.2.0"
  },
  "browserslist": [
    "> 1%",
    "last 2 versions",
    "not dead",
    "not ie 11"
  ]
}
